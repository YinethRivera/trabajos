/*let mul = parseInt(prompt("Ingresa el numero que deseas multiplicar:"));
for (let m = 0; m < 21; m++ ){
let respuesta= mul * m;
console.log(mul + '*' + m + '=' + respuesta )
}*/


let suma= 0 ;
let numeros= [1, 2, 3 , 4 , 5, 6, 7 , 8];
for (let n = 0; n <numeros.length; n++) {
    suma += numeros [n]
}
 console.log("suma de los elementos: "  + suma  )